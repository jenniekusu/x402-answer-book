# =============================================================================
# Answer Book Agent - Environment Variables
# =============================================================================
# Copy this file to .env and populate your own settings

# =============================================================================
# Agent Metadata
# =============================================================================
AGENT_NAME=answer-book
AGENT_VERSION=0.0.1
AGENT_DESCRIPTION=Answer Book - A mystical AI oracle book that provides philosophical answers to your questions

# Open Graph tags (optional, used for social sharing)
# AGENT_IMAGE=https://your-domain.com/og-image.png
# AGENT_URL=https://your-domain.com

# =============================================================================
# LLM Provider Configuration (configure at least one)
# =============================================================================

# OpenAI
OPENAI_API_KEY=sk-your-openai-api-key

# Or use Anthropic
# ANTHROPIC_API_KEY=sk-ant-your-anthropic-api-key

# Default model (optional, defaults to gpt-4)
# LLM_MODEL=gpt-4
# LLM_MODEL=gpt-3.5-turbo
# LLM_MODEL=claude-3-5-sonnet-20241022

# =============================================================================
# Payment Configuration (x402) - Base Mainnet
# =============================================================================

# x402 Facilitator endpoint
FACILITATOR_URL=https://facilitator.world.fun

# Payment network (currently Base Mainnet only)
NETWORK=base

# Receivable address (your EVM wallet)
PAYMENTS_RECEIVABLE_ADDRESS=0xYourWalletAddress

# Wallet private key (used for signing—keep it safe!)
PRIVATE_KEY=0xYourPrivateKey

# =============================================================================
# ERC-8004 Identity Configuration (optional)
# =============================================================================
# Configure the following if your agent is registered on the Base Mainnet ERC-8004 registry

# Your Agent ID in the ERC-8004 registry
# AGENT_ID=1

# Your Agent address (CAIP-10 format)
# AGENT_ADDRESS=eip155:8453:0xYourAgentAddress

# =============================================================================
# Server Configuration
# =============================================================================

# Server port (defaults to 3000)
PORT=3000
